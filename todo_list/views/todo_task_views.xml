<odoo>
    <record id="view_todo_task_form" model="ir.ui.view">
        <field name="name">todo.task.form</field>
        <field name="model">todo.task</field>
        <field name="arch" type="xml">
            <form string="Todo Task">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="is_done"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_todo_task_tree" model="ir.ui.view">
        <field name="name">todo.task.tree</field>
        <field name="model">todo.task</field>
        <field name="arch" type="xml">
            <list string="Todo Task">
                <field name="name"/>
                <field name="is_done"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="action_todo_task" model="ir.actions.act_window">
        <field name="name">Todo Tasks</field>
        <field name="res_model">todo.task</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_todo_task_tree"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first todo task!
            </p>
        </field>
    </record>

    <menuitem id="menu_todo_root" name="Todo" sequence="10"/>
    <menuitem id="menu_todo_task" name="Tasks" parent="menu_todo_root" action="action_todo_task" sequence="10"/>
</odoo>
